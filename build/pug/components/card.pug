include /components/image.pug
mixin card(service = {}, even = false)
  -
    let url = service.url;
    let title = service.title;
    let description = service.description;
    let { image } = service;
    
  +link({
    href: url,
    class: `${!even ? "md:pr-8" : ""} text-white md:max-w-1/2 hover:border border-solid border-red card col block intro-animation cursor-pointer group pb-8`
  })
    section.rounded-lg.relative.overflow-hidden.h-72.bg-primary
      +image(image)
      section.lackluster
        +image(image)
      .p-5.left-half.w-full.text-shadow.gra
        h3.text-xl.mb-3.font-extrabold= title
        //- (class="group-hover:underline")
        p.line-clamp= description
