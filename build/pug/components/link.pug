mixin link(arg = {})
  -
    let { content, href } = arg;
    let _class = arg.class || "layout-inline";
    let _href = href ?? "/";
  a(href=_href class=_class)&attributes(attributes)= content
    block
